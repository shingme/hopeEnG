<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AdminMapper">

	<select id="AdminMapper.Admin_select" parameterType="org.hope.web.admin.AdminVO" resultType="java.util.HashMap">
		SELECT ADMIN_ID
		     , ADMIN_PW
			 , ADMIN_NM
			 , ADMIN_PHONE
			 , ADMIN_EMAIL
			 , LOGN_DATE
	      FROM ADMINTB
	     WHERE ADMIN_ID = #{adminId}
	       AND ADMIN_PW = #{adminPw}
	</select>
	
	<update id="AdminMapper.Admin_login" parameterType="java.util.HashMap">
		UPDATE ADMINTB
		   SET LOGN_DATE = SYSDATE()
		 WHERE ADMIN_ID = #{adminId}
		   AND ADMIN_PW = #{adminPw}
	</update>
	

	 
</mapper>