<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BraaMapper">

    <select id="BRCC0100_mainInsert" parameterType="org.hope.web.domain.BraaVO">
        <!-- insert into testTable(name) values (#{name}) -->
        INSERT INTO BRODTB(USER_NM)
		VALUES (#{name});
		<!-- INSERT INTO BRODTB(BROD_NM, BROD_CTS, BROD_STUS, USER_NM, USER_EMAIL, USER_PHONE, USER_PW, REGI_DATE, MODY_DATE)
		VALUES(#{BROD_NM}, #{BROD_CTS},#{BROD_STUS},#{USER_NM},#{USER_EMAIL},#{USER_PHONE},#{USER_PW}, SYSDATE(), SYSDATE()); -->
    </select>

</mapper>